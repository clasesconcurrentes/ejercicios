package main

import (
	"fmt"
	"io/ioutil"
)

func search(file, name string) string {

	if file[i]=name{
	
	msf =file[i]
}
	return msf

}
func sort(mes string) {
	mesorder := string
	fmt.Println(mesorder)
}
func main() {
	file, error := ioutil.ReadFile("users.txt")
	if error != nil {

		fmt.Print(error)
	}
	message = search(file, "name")
	sort(message)

}
